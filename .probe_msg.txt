fix: 兑现已声明契约——请求体拦截真实链路、关键词命中过滤与文档漂移

请求体 1MB 拦截改用 starlette 内置 RequestBodyLimitMiddleware：
自建中间件在分块分支抛出的异常逃出 FastMCP 子应用被渲染为 500，
与 TASK-010 验收标准冲突；内置中间件声明式与分块式两条路径都返回
413，且本就在依赖内。删除 capsa/middleware.py。

关键词检索的命中判据与总分解耦：置顶（+3）与过期复核（-2）是排序
权重而非命中依据，此前"置顶但无词元命中"的条目净分为 3 被错误召回。
新增 retrieval._hits 独立谓词，score() 增量保持不变。

memory_read 在共享的 format_read 处将负 offset 归零，offset 越界时
输出结尾提示；memory_search 的 limit 报错改为取值范围表述。CLI
group add 对已存在 slug 返回 1 并提示未修改。

同步归档文档与 Project.md：修正归档目录名、§7 引用与已被证伪的
"必须显式传 path=/"结论，并补齐对应测试用例。

回归：.venv/bin/python -m pytest -q → 64 passed；真实 uvicorn 上裸
socket 复验 1.5MB 分块与 1.1MB 声明式请求均返回 413。